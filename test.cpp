#include <bits/stdc++.h>
using namespace std;
int main() {
  // FILE *f = fopen("2_yacc/parse/data/grammar.in", "r");
  // char s[1000];
  // int cnt = 0;
  // while (1) {
  //   cnt++;
  //   if(fscanf(f, "%s", s)==EOF) break;
  //   printf("%s\n", s);
  //   if (cnt > 100) break;
  // }
  vector<int> v=vector(1,2);
  for(auto it: v) {
    printf("%d\n",it);
  }
  return 0;
}