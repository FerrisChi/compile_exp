#include <bits/stdc++.h>
using namespace std;
int sum(int count, ...) {
  
}
int main() {
  // char str[100];
  // char *beginp, reBufp, forwardp, ;
  // int len = 0;
  // beginp = str, reBufp = str+4;
  // str[0] = '\0';
  // if (beginp < reBufp) {
  //   strncpy(str, beginp, reBufp - beginp);
  //   len += reBufp - beginp;
  // }
  // if (forwardp != lsBufp) {
  //     strncpy(tmp, lsBufp, forwardp - lsBufp);
  //     strcat(str, tmp);
  //     len += forwardp - lsBufp;
  //   }
  return 0;
}